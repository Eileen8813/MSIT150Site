@{
    ViewData["Title"] = "Home Page";
}
@section Styles{
    <style>
        .display-4 {
            background-color: aquamarine;
        }
    </style>
}


<div class="text-center">
    <h1 class="display-4">Welcome</h1>
    <p>Learn about <a href="https://docs.microsoft.com/aspnet/core">building Web apps with ASP.NET Core</a>.</p>
</div>
<table id="empTable" class="table table-bordered table-hover table-striped">
    <thead>
        <tr>
            <th>姓名</th>
            <th>年資</th>
            <th>薪水</th>
        </tr>
    </thead>
    <tbody></tbody>


</table>

@section Scripts{
    <script>
        const emp = {
            "employees": [
                { "name": "Tom", "workYears": 3, "salary": 35000 },
                { "name": "Jack", "workYears": 5, "salary": 40000 },
                { "name": "Mary", "workYears": 7, "salary": 45000 }
            ]
        };

        //const datas = emp.employees.map(employees => {
        //    const { name, workYears, salary } = employees
        //    return (
        //        `
        //            <tr>
        //                <td>${name} </td>
        //                        <td> ${workYears} </td>
        //                 <td> ${salary} </td>
        //            </tr>
        //        `
        //    )

        //});


        //const datas = $.map(emp.employees,employees => {
        //    const { name, workYears, salary } = employees
        //    return (
        //        `
        //                    <tr>
        //                        <td>${name} </td>
        //                                <td> ${workYears} </td>
        //                         <td> ${salary} </td>
        //                    </tr>
        //                `
        //    )

        //});
        $('#empTable>tbody').html(($.map(emp.employees, employees => {
            const { name, workYears, salary } = employees
            return (
                `
                                    <tr>
                                        <td>${name} </td>
                                                <td> ${workYears} </td>
                                         <td> ${salary} </td>
                                    </tr>
                                `
            )

        })).join(''));

        //document.querySelector('#empTable>tbody').innerHTML = datas.join('');
    </script>

}
